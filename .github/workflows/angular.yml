name: Angular Test, Build and Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/angular/**'
  pull_request:    
    branches:
        - main
    paths:
      - 'frontend/angular/**'       
   
jobs:
  test:
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    name: Run Tests for Angular
    uses: ./.github/workflows/test.yml
    with:
      microservice: angular
    secrets: inherit

  # Build Docker Image for React
  build:
    if: github.event_name == 'push' 
    name: Build React Docker Image
    uses: ./.github/workflows/build-and-push.yml
    with:
      microservice: angular
    secrets: inherit

  # Deploy React Microservice
  deploy:
    if: github.event_name == 'push' 
    name: Deploy React Microservice
    needs: build
    uses: ./.github/workflows/deploy-jobs.yml
    with:
      microservice: angular
    secrets: inherit